import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com'],   // add the host of your image URL
    // If you have other external hosts add them here too
  },
};

export default nextConfig;
